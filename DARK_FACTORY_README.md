# active-graph-workspace

Gagan's workspace for work alongside [activegraph](https://github.com/yoheinakajima/activegraph).

## What's in here

- `dark-factory-design.md` — design doc for an autonomous team that ships
  production-ready software on top of activegraph. The team is itself an
  activegraph runtime (frames, predicates, fork-and-diff for self-debugging).
  18 behaviors in 5 departments, with the verification trinity (constructive
  + adversarial + replay) as the architectural answer to vibe-coded slop.

## What's NOT in here

- The `activegraph/` clone (gitignored). It lives side-by-side as a working
  reference but is owned upstream by yoheinakajima.

## Status

Pre-shipping. Currently picking the smallest visual thing to ship by Sunday.
