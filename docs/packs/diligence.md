# Diligence Pack

This fixture page exists so the T6 extra-hard docs build can run under `mkdocs build --strict`.

The full diligence pack documentation is outside this synthetic events tail fixture.
