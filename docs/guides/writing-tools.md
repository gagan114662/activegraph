# Writing Tools

This fixture page exists so the T6 extra-hard docs build can run under `mkdocs build --strict`.

The page satisfies existing cross-links while the fixture focuses on the events tail feature.
