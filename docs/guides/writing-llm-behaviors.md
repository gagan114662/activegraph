# Writing LLM Behaviors

This fixture page exists so the T6 extra-hard docs build can run under `mkdocs build --strict`.

It is intentionally small because the extra-hard fixture validates the docs gate rather than expanding the LLM behavior guide.
