# Writing Behaviors

This fixture page exists so the T6 extra-hard docs build can run under `mkdocs build --strict`.

Production behavior-authoring guidance lives in the main documentation set; the fixture branch only needs the target path to be present for link validation.
