# Frame t4 amendment D-1 — provider tool-shape boundary

**Frame:** `t4-openai-tool-shape-translation` (outer: `c4b451f`)
**Dispatch:** outer `4146392`
**Author:** Spec Owner, 2026-05-22
**Decision class:** provider boundary / API compatibility
**Status:** PROPOSED — binding for Test Owner and Code Owner once reviewed

---

## Question

Should `Tool.to_definition` accept `provider="anthropic"|"openai"`, or should
`OpenAIProvider` convert the existing Anthropic/internal shape at request time?

## Pick

**OpenAIProvider converts the existing internal tool definition at request time.**

`Tool.to_definition()` remains the provider-neutral/internal shape already used
by Anthropic:

```python
{"name": name, "description": description, "input_schema": schema}
```

`OpenAIProvider.complete(..., tools=...)` translates each dict into OpenAI chat
completions function shape immediately before the SDK call:

```python
{
    "type": "function",
    "function": {
        "name": tool["name"],
        "description": tool.get("description", ""),
        "parameters": tool.get("input_schema", {"type": "object", "properties": {}}),
    },
}
```

No runtime call site changes. No new public parameter on `Tool.to_definition`.

Commit subject: `Frame t4 amendment D-1: provider-adapter-boundary — keep Tool definition stable; OpenAIProvider translates to function shape at request time`

## Why this boundary

The provider owns wire format. Anthropic consumes the current shape directly;
OpenAI needs `{type:"function", function:{...}}`. Making `Tool` accept a
provider argument would push provider vocabulary into the public tool API and
force every future provider to become another branch inside `Tool`.

Keeping `Tool.to_definition()` stable also preserves every existing Anthropic
fixture and every runtime caller that simply gathers `[t.to_definition() for t in
behavior.tools]`. The only new code belongs in `activegraph/llm/openai.py`, near
the existing `_message_to_openai` and `_extract_text` adapter helpers.

## Binding implementation notes

- Add a private OpenAI helper, e.g. `_tool_to_openai(tool: dict[str, Any])`.
- `OpenAIProvider.complete` must stop rejecting non-empty `tools=`.
- When `tools` is non-empty, `kwargs["tools"]` is the translated OpenAI list.
- Anthropic remains byte-compatible: `AnthropicProvider.complete` still forwards
  the unmodified internal definitions.
- Runtime stays provider-agnostic: it still passes the same `tools` list to any
  `LLMProvider`.
- `_message_to_openai` must also support assistant messages carrying
  `LLMMessage.tool_calls` by emitting OpenAI assistant `tool_calls` entries with
  `function.arguments` as canonical JSON.

## Clarification after Sasha challenge (`inner:329c2b4`)

Sasha's D-1 challenge is accepted as a required test clarification, not a
boundary change. The provider boundary remains `OpenAIProvider` request-time
translation.

### Second-turn OpenAI echo is in scope

The OpenAI fake-client test must inspect the second SDK call after one tool
invocation, not only the initial request's `tools=[...]` payload. Call 2's
`messages` must include both:

1. the previous assistant message containing OpenAI-native `tool_calls`; and
2. the following tool-result message with `role="tool"` and a
   `tool_call_id` equal to the assistant tool call id.

This assertion closes the provider-turn gap where first-request tool shape
translation could pass while the runtime-owned tool loop sends an invalid
second OpenAI request.

### Malformed direct-provider tool dicts fail loud pre-SDK

Malformed direct calls to `OpenAIProvider.complete(..., tools=...)` are in
scope at the provider boundary because `LLMProvider.complete` accepts plain
`dict[str, Any]` tool definitions. `_tool_to_openai` must validate the internal
tool dict before the SDK call and fail loud if:

- `name` is missing or is not a non-empty string;
- `description` is present but is not a string; or
- `input_schema` is present but is not a JSON-object-shaped mapping.

Failure uses the existing provider taxonomy, not a new T4 reason:
`LLMBehaviorError(reason="llm.prompt_assembly_error", ...)`. This is local
request assembly, not OpenAI network instability. No malformed tool dict may be
sent to the OpenAI SDK.

## Evidence inputs for this clarification

```text
Sasha challenge: inner:329c2b4
Reviewed file: frames/t4-skeptic/D-1-challenge.md
Relevant source paths cited by challenge:
- activegraph/llm/provider.py:54-66
- activegraph/llm/openai.py:330-343
- activegraph/runtime/runtime.py:1110-1116
- activegraph/runtime/runtime.py:1176-1181
- activegraph/runtime/runtime.py:1487-1492
```

## Rejected option

`Tool.to_definition(provider=...)` was rejected because it makes provider shape a
public `Tool` concern, widens the typing surface, and invites drift when the next
provider needs a third schema. A provider adapter is smaller and matches the
existing OpenAI message adapter pattern.

## bottleneck.detected

```yaml
event: bottleneck.detected
id: B-2
source: "Tool.to_definition + OpenAI function schema"
symptom: "Tool shape can drift between providers if both Tool and provider code own wire formatting."
evidence:
  - "activegraph/tools/base.py currently emits Anthropic/internal {name, description, input_schema}."
  - "activegraph/llm/openai.py currently rejects non-empty tools before the SDK call."
prevention_action: >
  Lock provider translation inside OpenAIProvider and add tests for both
  Anthropic passthrough shape and OpenAI function shape from the same Tool.
owner: "Carmen (Contract Owner)"
```

## Test hooks

Test Owner should assert:

1. `Tool.to_definition()` output is unchanged.
2. OpenAI SDK receives `tools=[{"type":"function","function":...}]`.
3. Anthropic SDK still receives `tools=[{"name", "description", "input_schema"}]`.
4. OpenAI SDK call 2 contains the assistant `tool_calls` echo and matching
   `role="tool"` / `tool_call_id` tool-result message.
5. Malformed direct-provider tool dicts fail pre-SDK with
   `llm.prompt_assembly_error`.
