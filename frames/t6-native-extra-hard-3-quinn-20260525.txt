NATIVE_GAUNTLET EXTRA_HARD T6_NATIVE_EXTRA_HARD_20260525 STEP3 QUINN_TESTS

Quinn, you are Test Adversary for the T6 extra-hard feature: activegraph events tail.

IMPORTANT WORKTREE BOUNDARY:
- Work ONLY in this clean inner-repo worktree:
  /tmp/t6-extra-hard-live-run-activegraph
- Do NOT edit /Users/gaganarora/Desktop/my projects/active_graph/activegraph; it has unrelated dirty operator work.
- Commit your changes in /tmp/t6-extra-hard-live-run-activegraph on branch t6-extra-hard-live-run-20260525.
- Do NOT fix implementation code. Maya owns the fix after your adversarial tests land.
- Do NOT trigger Maya or any other agent. The operator will hand off after your ACK.

Prior output:
- Sofia spec path: docs/specs/events-tail.md
- Sofia ACK id: 007ae6a7-51d5-4ff9-903a-799c479ccc15
- Maya implementation commit: 17b891f
- Maya ACK id: 44df2679-bb2d-47b7-a3fd-d8162cbd6def
- Maya implementation paths: activegraph/cli/events_tail.py, activegraph/cli/main.py

Feature scope:
Adversarially test the activegraph events tail CLI subcommand implemented from Sofia's spec. It prints the last N events from the active event store as newline-delimited JSON. Flags: --n <int> default 20, --since <iso-timestamp>, --filter <substring>, and --json. Each output row must include id, ts, kind, payload, parent_id. The command itself must emit an events_tail_invoked audit event when invoked.

Deliverables:
1. Read docs/specs/events-tail.md and Maya's implementation.
2. Write tests only in:
   tests/test_cli_events_tail.py
3. Add at least 6 focused tests. Required coverage:
   - default tail emits newline-delimited JSON rows with id, ts, kind, payload, parent_id
   - --n returns the last N matching events and includes the audit event when it matches
   - --since filters using ISO timestamps
   - --filter is a literal substring filter over event kind
   - no active store exits non-zero with a stderr message containing no active event store
   - malformed flags are rejected before events_tail_invoked is appended
4. Include at least one adversarial test that fails against Maya's current implementation and demonstrates a real spec violation.
5. Run the focused tests against the current Maya implementation and capture the failing output in your commit message body or a short comment at the top of the test file.
6. Commit only the test file. Commit message:
   T6 extra-hard: Quinn adversarial events tail tests

ACK:
Reply exactly after committing tests:
QUINN_TESTS_DELIVERED T6_NATIVE_EXTRA_HARD_20260525 STEP3 activegraph/tests/test_cli_events_tail.py T6XH_QUINN_FINDING_EVENTS_TAIL_20260525 parent_ack_id=44df2679-bb2d-47b7-a3fd-d8162cbd6def
